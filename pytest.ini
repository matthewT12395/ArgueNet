[pytest]
testpaths = tests
markers =
    unit: Fast local tests with mocked dependencies.
    integration: Tests that exercise multiple components together.
    kafka: Tests that require a reachable Kafka broker.
    timeout: Per-test timeout marker.
addopts = -m "not kafka"
